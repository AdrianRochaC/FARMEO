// Configuración de PM2 para producción
// Este archivo permite iniciar el servidor con: pm2 start ecosystem.config.js

module.exports = {
  apps: [{
    name: 'capacitaciones-backend',
    script: 'server.js',
    instances: 1,
    autorestart: true,
    watch: false,
    max_memory_restart: '1G',
    env: {
      NODE_ENV: 'production',
      PORT: 3001
    },
    error_file: './logs/err.log',
    out_file: './logs/out.log',
    log_date_format: 'YYYY-MM-DD HH:mm:ss Z',
    merge_logs: true
  }]
};


